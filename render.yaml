services:
  - type: web
    name: graph-calculator
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn graph_calculator:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0